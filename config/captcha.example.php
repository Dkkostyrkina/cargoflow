<?php
declare(strict_types=1);

/**
 * Конфигурация Yandex SmartCaptcha.
 *
 * Где взять ключи: https://console.yandex.cloud → SmartCaptcha → создать капчу.
 * После создания доступны два ключа:
 *   - клиентский (публичный) — для виджета на странице;
 *   - серверный (приватный)  — для проверки токена на бэкенде.
 *
 * Для локальной отладки Yandex предоставляет тестовые ключи (см. документацию
 * SmartCaptcha): клиентский ключ всегда показывает капчу, серверный — всегда
 * подтверждает прохождение.
 *
 * Если ключи не заданы (пустые строки), проверка капчи отключается, а формы
 * продолжают работать в обычном режиме (graceful degradation, как и для БД/AI).
 *
 * Чтобы включить капчу: скопируйте этот файл в config/captcha.php
 * и подставьте свои ключи.
 */
const CF_CAPTCHA_CLIENT_KEY = '';
const CF_CAPTCHA_SERVER_KEY = '';

// Тестовый режим: капча всегда показывает задание (для отладки и скриншотов).
// На реальном сайте оставьте false.
const CF_CAPTCHA_TEST_MODE = false;
